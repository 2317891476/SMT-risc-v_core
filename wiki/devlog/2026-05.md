# 2026-05 Devlog

Append-only, newest entries first.

## 2026-05-25 Sv32 MMU Standalone And Scaffold

- Saved the previous Linux foundation as commit `dce54c1` (`Add Linux bring-up foundation`), excluding generated Vivado report files.
- Reworked `rtl/mmu_sv32.v` around the Linux bring-up interface: a simple physical PTW request/response port, M-mode/bare bypass, 4KB and 4MB Sv32 walking, U/S/SUM/MXR permission checks, hardware PTE A/D writeback, page-fault cause/tval outputs, and `sfence.vma` TLB flushing.
- Updated `rtl/tlb.v` so refilling an existing VPN/ASID overwrites the stale entry instead of inserting a duplicate. This is required when a load fills a clean PTE and a later store must update the same TLB entry with the D bit set.
- Added `comp_test/tb_mmu_sv32.sv` and module-level aliases in `verification/run_all_tests.py`: `test_sv32_translation`, `test_sv32_page_faults`, and `test_sfence_vma`.
- I/D insertion: `sifang_core` now connects CSR `satp`, privilege, `mstatus.MXR`, and `mstatus.SUM` outputs to `u_mmu_sv32`. `stage_if` translates virtual fetch PCs before `inst_memory/icache`; `lsu_shell` translates load/store/AMO effective addresses before store-buffer lookup/enqueue and M1 requests.
- Fixed the privilege smoke after real MMU insertion: `test_priv_mret_sret_delegation` previously wrote `satp=0x80000001` without page tables, which only worked while MMU was disconnected. It now keeps `satp.MODE=0`; MODE=1 execution is covered by the Sv32 tests that install page tables.
- Verification after I/D insertion: `python verification\run_all_tests.py --tests test_sv32_translation test_sv32_page_faults test_sfence_vma --fpga-config` passed 3/3; `python verification\run_all_tests.py --basic` passed 41/41; `python verification\run_all_tests.py --basic --fpga-config` passed 41/41; `python verification\run_riscv_tests.py --suite riscv-tests --categories rv32ua` passed 10/10.
- Verilator protection after I/D insertion passed both board-equivalent Dhrystone gates. Preload 5000-run reported `BoardConfigMatch=True`, `PreloadBenchmarkPass=True`, `DHRYSTONE DONE`, no trap/stuck/unexpected UART. Loader-semantic 5000-run reported `BoardConfigMatch=True`, `LoaderSemanticPass=True`, 128 block ACKs, `DHRYSTONE DONE`, no trap/stuck/unexpected UART, and `ExitReason=done`.
- Remaining blockers before Linux boot: add the `mem_subsys` PTW physical port/arbitration and carry fetch/load/store page faults into CSR at ROB-precise commit.

## 2026-05-25 Linux MMU Bring-up Foundation

- Started the SifangCore Linux path as MMU Linux: `RV32IMA_Zicsr_Zifencei`, S-mode, Sv32, OpenSBI, Linux, and BusyBox initramfs. NoMMU Linux is not the target.
- Added/validated Linux prerequisite RTL paths: S-mode CSR/delegation/SRET, RV32A AMO/LRSC, PLIC UART source 2 through S-context, and OpenSBI-style STIP injection into S-mode timer interrupt.
- Fixed a CSR delegation bug found during bring-up: the previous `exc_cause[4:0] < 5'd32` check made delegation impossible because `5'd32` truncates to zero. Removing that check allowed S-mode ECALL delegation to reach `stvec`.
- Added directed tests `test_priv_mret_sret_delegation`, `test_amo_lrsc`, `test_plic_s_context_uart_irq`, and `test_sbi_timer_injection`.
- Verification: `python verification\run_all_tests.py --basic` passed 41/41; `python verification\run_all_tests.py --basic --fpga-config` passed 41/41; `python verification\run_all_tests.py --tests test_plic_external_interrupt test_plic_retrigger test_uart16550_plic_irq test_plic_s_context_uart_irq test_priv_mret_sret_delegation test_amo_lrsc -v` passed 6/6; `python verification\run_all_tests.py --tests test_sbi_timer_injection test_priv_mret_sret_delegation test_plic_s_context_uart_irq -v` passed 3/3.
- Extended classic riscv-tests coverage to RV32A. `python verification\run_riscv_tests.py --suite riscv-tests --categories rv32ua` passed 10/10. `rv32ua/lrsc` originally timed out in the default 1ms testbench window while still executing the 1024-iteration LR/SC loop; increasing the riscv-tests TB timeout to 10ms resolved the false failure. `python verification\run_all_tests.py --riscv-tests` also passed.
- Board-equivalent WSL Verilator Dhrystone preload after the Linux prerequisite changes passed with `BoardConfigMatch=True`, `PreloadBenchmarkPass=True`, no trap, no stuck PC, and no unexpected UART. A 5000-run loader-semantic board-equivalent Dhrystone pass was also recorded earlier in this same debug session before the linux-preload harness extension.
- Added `software/linux/` staging: AX7203 DTS, minimal initramfs `/init`, and WSL build script. The script currently produces `build/linux/sifangcore_ax7203.dtb` and `build/linux/rootfs.cpio`, and intentionally refuses to claim `fw_payload.bin` until OpenSBI/Linux/BusyBox sources and configs are ready.
- Added Verilator `linux-preload` mode and UART token detection for `OpenSBI`, `Boot HART ID: 0`, `Linux version`, `Run /init as init process`, and `SIFANGCORE LINUX PASS`. The mode currently fails early if `build/linux/fw_payload.bin` is absent, which is expected.
- Remaining Linux blockers: `mmu_sv32` is not wired into fetch/LSU, PTW has no integrated memory-system port, precise page-fault causes are not fully plumbed to CSR, and PTE A/D update behavior is not integrated.

## 2026-05-24 SifangCore Structural Rename Started

- Project identity changed to **SifangCore: A RISC-V Processor Core**.
- Chosen scope is a full structural rename with no legacy compatibility entrypoints: core modules/files/scripts/plugin names use `sifang_core`, FPGA top stems use `sifang_core_ax7203`, and UART boot banners use `SifangCore AX7203 Boot`.
- Because module names, hierarchy paths, Tcl defaults, RISCOF plugin names, and board artifact stems changed, post-rename validation must rerun Icarus, WSL Verilator Dhrystone, and then Vivado 2023.2 incremental/JTAG/COM5 board validation. Pre-rename bitstream `0x6A12D692` remains historical evidence only.
- Post-rename validation progress: Python script syntax checks passed; old-name source/document search is clean; Icarus directed FPGA-config tests passed 3/3; `--basic` and `--basic --fpga-config` both passed 37/37.
- WSL Verilator post-rename Dhrystone passed both board-equivalent 5000-run gates. Preload reported `BoardConfigMatch=True`, `PreloadBenchmarkPass=True`, no trap/stuck/unexpected UART. Loader-semantic reported `BoardConfigMatch=True`, `LoaderSemanticPass=True`, 128 block ACKs, `DHRYSTONE DONE`, no trap/stuck/unexpected UART.
- First Vivado 2023.2 post-rename incremental attempt synthesized `sifang_core_ax7203_post_synth.dcp`, but the pre-rename routed reference only matched 7.65% of cells and 8.28% of nets. Vivado disabled incremental mode and ran default implementation, which routed but failed timing badly (`WNS=-30.855`, `WHS=-5.037`) and reported routing resource conflicts in the final route-status report. This is a low-reuse/tool-flow failure caused by the structural rename, not a Dhrystone functional regression.
- Updated `fpga/impl_incremental.tcl` so unavailable pre-place incremental reuse data fails immediately instead of allowing Vivado to fall through into a long default place/route cycle. The first fix exposed a stale-report bug: when incremental mode is disabled, Vivado may leave the old reuse report untouched. The Tcl now deletes reuse reports before regenerating them and checks the `report_incremental_reuse` status.
- The required post-failure Verilator boundary matrix passed after the two incremental failures: preload/read-only/mock-latency7 passed with `BoardConfigMatch=True`; preload/full/mock-latency1 passed with only the deliberate `DCACHE_MODE` mismatch; loader-semantic/read-only/mock-latency7 passed with `BoardConfigMatch=True`, 128 block ACKs, and `DHRYSTONE DONE`.
- Because the full structural rename leaves no same-name routed reference for incremental reuse, the next implementation action is an explicit Vivado 2023.2 aggressive seed run to create a new `sifang_core_ax7203` routed checkpoint and bitstream for future incremental debug.
- Vivado 2023.2 aggressive seed run succeeded for `sifang_core_ax7203_top`: build ID `0x6A132BA3`, timing met with `WNS=+0.403` and `WHS=+0.051`, and bitstream `build/ax7203/sifang_core_ax7203_xc7a200tfbg484-2.bit` was generated. The first combined aggressive run passed smoke but the baseline capture missed `DHRYSTONE DONE` within 240 seconds after already loading and jumping.
- A follow-up `--fpga-impl-mode board-only` run using the same bitstream passed. Vivado JTAG DONE/EOS succeeded and USR_ACCESS/USERCODE both read back `0x6A132BA3`; COM5 UART smoke and 5000-run baseline both reached `DHRYSTONE DONE`, with loader summary mask `0x1F`.

## 2026-05-24 Documentation GitHub Submission Rule

- Added a repository rule to `AGENTS.md`: wiki and project documentation changes must be included in the same Git/GitHub commit or PR as the related RTL/script/debug change. If the current turn is not authorized to commit or push, the agent must explicitly report the documentation files that need to be included in the next GitHub submission.
- Synced the same rule into `CLAUDE.md` and the wiki quick card so future agents do not leave `wiki/` or documentation edits as unsubmitted local-only changes.

## 2026-05-24 5000-Run Dhrystone Gate Tightened

- Board-only run of fresh bitstream `0x6A12D692` used Vivado 2023.2 JTAG twice. Both smoke and baseline programming logs show DONE/EOS and USR_ACCESS/USERCODE readback matching `0x6A12D692`.
- Smoke payload completed the full UART loader path and printed `DHRYSTONE DONE` for the 10-run fixed image. Baseline payload completed transport cleanly through block 127, emitted loader summary mask `0x1F`, jumped to DDR3, and printed `DHRYSTONE START`, but did not print `DHRYSTONE DONE` within the 240-second post-load capture.
- This rules out JTAG programming and UART loader transport as the active failure for the fresh bitstream. The current blocker is the 5000-run Dhrystone execution path after startup.
- Tightened `fpga/scripts/run_verilator_mainline.py`: board-equivalent config now includes `DHRYSTONE_RUNS=5000` and `BENCHMARK_RUNTIME_PROFILE=board`. The benchmark builder passes `--fixed-dhrystone-runs <runs>` so Verilator actually executes the requested run count instead of silently using the previous `VERILATOR_MAINLINE` 10-run shortcut, and board-equivalent runs no longer define the C-side `VERILATOR_MAINLINE` runtime fast path.
- Updated the Verilator harness benchmark-window detector from the literal `Trying 10 runs through Dhrystone:` to any ` runs through Dhrystone:` line, so 5000-run profiling and stuck detection work.
- Re-ran board-equivalent WSL Verilator. Preload 5000-run passed with `BoardConfigMatch=True`, `BenchmarkRuntimeProfile=board`, `PreloadBenchmarkPass=True`, and `DHRYSTONE DONE`. Loader-semantic with the old 1.2B budget timed out while still retiring at the budget boundary; rerunning with `--max-cycles 2000000000` passed with `LoaderSemanticPass=True`, 128 block ACKs, `BENCH CYCLES: 19467034`, `DHRYSTONE DONE`, no trap, no unexpected UART, no stuck PC, and no retire stall.
- Re-ran board-only validation with the existing Vivado 2023.2 incremental bitstream `0x6A12D692`. Result: PASS. Smoke and baseline JTAG downloads both reported DONE/EOS and USR_ACCESS/USERCODE readback `0x6A12D692`; smoke printed `DHRYSTONE DONE`; baseline 5000-run printed `BENCH CYCLES: 35618852`, `BENCH INSTRET: 4915092`, and `DHRYSTONE DONE`. The previous baseline 240-second miss is now superseded by this clean board rerun.

## 2026-05-24 WSL Restored And Loader Gate Passed

- User showed that an external PowerShell could run `wsl`, then `wsl.exe -l -v` reported `Ubuntu-22.04 Running` and `docker-desktop Stopped`. Rechecked from Codex: `wsl.exe bash -lc` now sees user `illya`, Verilator 5.046 at `/usr/local/bin/verilator`, `/usr/bin/make`, and `/usr/bin/g++`. Conclusion: the earlier failure was session/WSL distro registration state, not the wrapper's `bash -lc` call shape.
- Board-equivalent Verilator preload Dhrystone passed with `BoardConfigMatch=True`, `ExitReason=done`, `PreloadBenchmarkPass=True`, benchmark START/DONE seen, no trap, and no unexpected UART.
- The default loader-semantic command is now intentionally rejected by board-config checking: it reports `PAYLOAD_START_GAP_CYCLES: expected 2500, actual 2`, `PAYLOAD_GAP_CYCLES: expected 2500, actual 2`, and `PAYLOAD_CHUNK_GAP_CYCLES: expected 40000, actual 2`.
- Root cause of the loader-semantic block-boundary failure: the board-profile loader cleared the 16550 RX FIFO at every `load_block_begin`, racing against the host's legal next-block payload after a block ACK. Removed that per-block `FCR` clear from `rom/test_fpga_ddr3_loader.s`; startup FIFO clear remains.
- After the ROM fix, loader-semantic prefix 2 passed with board ROM profile and host pacing `--payload-start-gap-cycles 2500 --payload-gap-cycles 2500 --payload-chunk-gap-cycles 40000`.
- Full loader-semantic Dhrystone passed with `--max-cycles 1200000000`: `BoardConfigMatch=True`, `LoaderSemanticPass=True`, 126 blocks ACKed, benchmark START/DONE seen, no trap, no unexpected UART, and `LoaderBytesInjected=8564`.
- Updated the Verilator board-config check to include loader-semantic host pacing, so fast host runs cannot authorize Vivado implementation.

## 2026-05-24 +08:00

- Attempted the required board-equivalent Verilator preload gate: `python fpga\scripts\run_verilator_mainline.py --mode preload --benchmark dhrystone --runs 1 --dcache-mode read-only --mock-latency 1 --require-board-config-match`.
- Result: blocked before simulation. `wsl.exe --status` reports WSL default version 2, but `wsl.exe -l -v` reports no installed Linux distributions; the wrapper therefore cannot find WSL-side `verilator`. Per the Dhrystone loop, Vivado implementation remains paused until a WSL distro with `verilator`, `make`, and `g++` is available.
- Updated `fpga/scripts/run_verilator_mainline.py` to decode localized UTF-16LE WSL failures explicitly, so future missing-distro/missing-tool errors are reported as environment blockers instead of Python `UnicodeDecodeError` noise.
- Tightened the documented board-equivalent Verilator configuration to include loader ROM profile `board`. Runs using `SIM_FAST_STORE_DRAIN` or `--loader-rom-profile sim-fast` are useful for directed simulation only and must not authorize AX7203 synthesis/implementation.
- Moved the Verilator board-config mismatch check before WSL tool probing. A negative check with `--loader-rom-profile sim-fast --require-board-config-match` now writes `BoardConfigMatch: False` and `LOADER_ROM_PROFILE: expected board, actual sim-fast` under `build/verilator/mainline/.../summary.txt` even though the current WSL Verilator environment is missing.
- Local fallback sanity passed while Verilator is blocked: `python verification\run_all_tests.py --tests test_uart16550_polling test_uart16550_plic_irq test_store_buffer_simple test_store_buffer_long_stream test_store_buffer_long_drain_poll --fpga-config` returned 5/5 PASS.

## 2026-05-22 11:33 +08:00

- Ran Vivado 2023.2 incremental implementation again after the board-matched Verilator matrix passed. Command used the existing post-synth checkpoint, the routed reference DCP, `AX7203_INCREMENTAL_DIRECTIVE=TimingClosure`, `AX7203_INCREMENTAL_PLACE_DIRECTIVE=ExtraNetDelay_high`, `AX7203_INCREMENTAL_ROUTE_DIRECTIVE=Explore`, and board parameters RS depth 48, fetch depth 16, 25 MHz, UART divider 217.
- Result: failed again in `place_design` at `Phase 3.3 Place Remaining` with `ERROR: [Place 30-99] Placer failed with error: 'failed to commit all instances'`. Vivado also warned that the changed area had few free physical sites.
- Reuse evidence: pre-place current cell reuse 89.00%; post-place current cell reuse 77.63%; post-place non-reused cells 22.36%, including 11.59% discarded to improve timing. Failed checkpoint was written to `build/ax7203/checkpoints/sifang_core_ax7203_incremental_place_failed.dcp`.
- Conclusion: this remains an incremental placement/local-site-congestion failure. It does not contradict the board-matched Verilator Dhrystone pass.

## 2026-05-22 10:03 +08:00

- Re-ran the Dhrystone Verilator gates after adding explicit board-config propagation and checking. Preload/read-only/mock-latency1 passed with `BoardConfigMatch=True`; loader-semantic/read-only/mock-latency1 passed with `--max-cycles 60000000`, `BoardConfigMatch=True`, `LoaderSemanticPass=True`, no trap, and no unexpected UART.
- Re-ran the post-incremental-failure boundary matrix under the new config reporting. Preload/read-only/mock-latency7 and loader-semantic/read-only/mock-latency7 both passed with `BoardConfigMatch=True`. Preload/full/mock-latency1 also passed and correctly reported only the deliberate `DCACHE_MODE: expected read-only, actual full` mismatch.
- The current Dhrystone functional simulation gate is board-matched and green. Next implementation work can proceed from Vivado/tooling evidence rather than RTL simulation uncertainty.

## 2026-05-22 09:59 +08:00

- User tightened the Dhrystone gate: Verilator runs must detect whether their build configuration matches the real AX7203 board-test configuration before authorizing synthesis/implementation.
- Updated `fpga/scripts/run_verilator_mainline.py` to pass board parameters into the Verilator build (`SMT_MODE`, RS depth/index width, fetch buffer depth, clock-wizard divider, and UART divider) and to emit `BoardConfigMatch`, expected config, actual config, and mismatch details in `summary.txt`/`summary.json`.
- Added `--require-board-config-match` for board-equivalent Verilator gates. The required baseline is single-thread, DDR3/mem_subsys enabled, RS depth 48, fetch buffer depth 16, 25 MHz, 115200 UART, UART divider 217, and read-only dcache. Boundary stress rows that deliberately vary a parameter, such as full dcache, must record the mismatch rather than masquerading as board-equivalent.
- Recorded the old-bitstream board-only result: Vivado 2023.2 JTAG programming/readback of build ID `0x6A0ED6D5` worked, but UART loader smoke failed during block 0 with `HW_RX_STALLED` and `HW_CORE_FLAGS=0xEA`. This remains stale-bitstream evidence only.
- Updated `fpga/impl_aggressive.tcl` so a failed aggressive implementation exits non-zero instead of looking successful to automation.

## 2026-05-22 00:36 +08:00

- Verilator boundary matrix passed after the two incremental failures: preload/read-only/mock-latency7, preload/full/mock-latency1, and loader-semantic/read-only/mock-latency7 with `--max-cycles 120000000` all finished with `ExitReason=done`, no trap, and no unexpected UART.
- This rules out the tested Dhrystone cache/latency simulation cases as the active blocker. The remaining short-term blocker is Vivado placement congestion or board-level UART/loader validation, not Dhrystone functional simulation.

## 2026-05-22 00:31 +08:00

- Second Vivado 2023.2 incremental retry used `AX7203_INCREMENTAL_DIRECTIVE=TimingClosure`, got past global placement, reached `Phase 3.3 Place Remaining`, then failed with the same `failed to commit all instances` placer error after about 55:40 elapsed in placement.
- This is now two consecutive incremental implementation failures. Per the Dhrystone loop, Vivado implementation retries are paused and the active next step is the Verilator boundary matrix.
- Updated `fpga/impl_incremental.tcl` failure text so it no longer points agents toward automatic aggressive fallback; it now points back to the Dhrystone debug loop.

## 2026-05-21 23:20 +08:00

- First post-Verilator Vivado 2023.2 incremental Dhrystone implementation attempt passed synthesis and incremental reuse DB creation, then failed during placement with `failed to commit all instances`; reuse reports showed 89.00% current cell reuse before placement and 77.63% after the failed placement.
- Stopped the benchmark script's automatic aggressive fallback so the Dhrystone loop stays Verilator-first and incremental-only until the two-failure threshold is reached.
- Added incremental directive environment knobs in `fpga/impl_incremental.tcl`. The one allowed retry after this placement failure will use `AX7203_INCREMENTAL_DIRECTIVE=TimingClosure`, `AX7203_INCREMENTAL_PLACE_DIRECTIVE=ExtraNetDelay_high`, and `AX7203_INCREMENTAL_ROUTE_DIRECTIVE=Explore`.
- Synced `CLAUDE.md` with the same WSL Verilator, Vivado 2023.2, fail-closed incremental, and Vivado JTAG rules so it no longer contradicts `AGENTS.md`.

## 2026-05-21 21:32 +08:00

- Implemented the Dhrystone-specific debug loop in `AGENTS.md`: WSL Verilator preload and loader-semantic Dhrystone gates first, Vivado 2023.2 incremental implementation second, and a hard fallback to the Verilator boundary matrix after two consecutive incremental failures.
- Synced `wiki/INDEX.md`, `wiki/concepts/simulation.md`, and `wiki/concepts/vivado-tooling.md` with the same loop so future RTL or board debugging does not skip the Verilator gate.
- Verilator result: preload/read-only/latency1 passed with `ExitReason=done` and no trap. Loader-semantic/read-only/latency1 timed out at the default 20M cycles while still making readback-loop progress, then passed with `--max-cycles 60000000` (`LoaderSemanticPass=True`, benchmark START/DONE seen, no trap or unexpected UART).

## 2026-05-21 20:58 +08:00

- User clarified the project-level target: a full-stack, competition-grade and industrial-grade dual-issue out-of-order processor core that can boot Linux and run benchmark/test programs with high performance.
- Updated `AGENTS.md` to make the debug ladder explicit: Icarus first, WSL Verilator through `fpga/scripts/run_verilator_mainline.py` second, then Vivado 2023.2 implementation and AX7203 board validation over Vivado JTAG.
- Updated wiki quick cards and simulation/tooling pages to match the required environment: Vivado 2023.2 at `E:\Xilinx\Vivado\2023.2\bin\vivado.bat`, Verilator through WSL, COM5 UART for board capture.

## 2026-05-21 20:49 +08:00

- User confirmed that Vivado 2024.2 exists on the machine but required this repo to continue using Vivado 2023.2 for implementation consistency.
- Added R1 wiki synchronization rule to `AGENTS.md` and `CLAUDE.md`.
- Created `wiki/` structure with `INDEX.md`, concept pages, this devlog, and a Canvas topology file.
- Current debug stage: single-thread RTL basic simulations and directed FPGA-config tests are passing; UART16550 and PLIC source 2 are implemented; loader long simulation passed after a branch-complete duplicate-pulse fix; Vivado 2023.2 synthesis succeeded; fresh board bitstream is still blocked by Vivado 2023.2 process-level implementation failures.

## 2026-05-21 Earlier

- A temporary dispatch debug-port expansion was ruled out after it triggered a Vivado 2023.2 synthesis/netlist translation crash. The temporary ports were reverted.
- The lower-risk debug beacon path through `mem_subsys` remains useful for board stalls.
- Old board bitstreams do not include the latest branch-complete fix and must not be used as proof that the current RTL works on hardware.
